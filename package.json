{
  "name": "kasasync",
  "version": "1.0.0",
  "private": true,
  "description": "KasaSync Real-Time Property Rental, Maintenance & Amenity Management Platform",
  "workspaces": [
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client"
  }
}
